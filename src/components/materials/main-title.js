export default function MainTitle({text}) {
    return (
        <div className="pb-10">
            <span className="text-[32px] font-bold md:text-[36px] lg:text-[40px] xl:text-[44px]">{text}</span>
        </div>
    )
}